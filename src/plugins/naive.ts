import { create,
  NLayout,
  NLayoutHeader,
  NLayoutSider,
  NLayoutContent,
  NMenu,
  NInput,
  NIcon
} from 'naive-ui'

export default create({
  components: [
    NLayout,
    NLayoutHeader,
    NLayoutSider,
    NLayoutContent,
    NMenu,
    NInput,
    NIcon
  ]
})
